import { BursonDashboard } from "@/components/burson-dashboard";
import { MobileShell, requireRole } from "@/components/mobile-shell";
export default async function BursonPage() { const role = await requireRole((item) => item.id === "burson" || item.id === "admin" || Boolean(item.bursonLinked)); return <MobileShell active="Burson" role={role}><main className="space-y-5 px-3 py-4 md:px-6 md:py-6 lg:px-7"><header className="technical-surface rounded-[10px] bg-night p-5 text-white md:p-7"><p className="relative text-[0.625rem] font-extrabold uppercase tracking-[0.2em] text-lime">Coordinación externa</p><h1 className="section-title relative mt-1 text-2xl md:text-3xl">Encargos Burson</h1><p className="relative mt-2 max-w-2xl text-sm text-white/65">Burson deja el encargo; el operario vinculado se responsabiliza de completarlo.</p></header><BursonDashboard role={role} /></main></MobileShell>; }
