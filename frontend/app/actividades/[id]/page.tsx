import { ActivityDetail } from "@/components/activity-detail";
import { MobileShell, requireRole } from "@/components/mobile-shell";
export default async function ActivityDetailPage({ params }: PageProps<"/actividades/[id]">) { const role = await requireRole(); const { id } = await params; return <MobileShell backHref={role.id === "burson" ? "/burson" : "/actividades"} role={role}><main className="mx-auto max-w-5xl space-y-4 px-3 py-4 md:px-6 md:py-6"><header className="border-b border-line pb-4"><p className="text-[0.625rem] font-extrabold uppercase tracking-[0.2em] text-cyan">Ficha de producción</p><h1 className="section-title mt-1 text-2xl">Detalle de actividad</h1></header><ActivityDetail id={id} role={role} /></main></MobileShell>; }
