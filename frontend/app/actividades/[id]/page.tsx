import { ActivityDetail } from "@/components/activity-detail";
import { MobileShell, requireRole } from "@/components/mobile-shell";
export default async function ActivityDetailPage({ params }: PageProps<"/actividades/[id]">) { const role = await requireRole(); const { id } = await params; return <MobileShell backHref={role.id === "burson" ? "/burson" : "/actividades"} role={role}><main className="space-y-3 px-3 py-4 md:px-6 md:py-6 lg:px-7"><header><p className="text-[0.6875rem] font-bold uppercase tracking-[0.16em] text-blue">Ficha de producción</p><h1 className="mt-1 text-2xl font-bold">Detalle de actividad</h1></header><ActivityDetail id={id} role={role} /></main></MobileShell>; }
