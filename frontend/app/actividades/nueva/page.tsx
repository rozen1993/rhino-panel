import { ActivityForm } from "@/components/activity-form";
import { MobileShell, requireRole } from "@/components/mobile-shell";
export default async function NewActivityPage({ searchParams }: PageProps<"/actividades/nueva">) { const role = await requireRole((item) => item.id === "operario"); const value = (await searchParams).editar; const activityId = Array.isArray(value) ? value[0] : value; return <MobileShell backHref={activityId ? `/actividades/${activityId}` : "/actividades"} role={role}><main className="space-y-4 px-3 py-4 md:px-6 md:py-6 lg:px-7"><header><p className="text-[0.6875rem] font-bold uppercase tracking-[0.16em] text-blue">Trabajo diario</p><h1 className="mt-1 text-2xl font-bold">{activityId ? "Editar actividad" : "Nueva actividad"}</h1></header><ActivityForm activityId={activityId} editing={Boolean(activityId)} role={role} /></main></MobileShell>; }
