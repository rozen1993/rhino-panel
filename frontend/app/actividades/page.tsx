import { ActivityDashboard } from "@/components/activity-dashboard";
import { MobileShell, requireRole } from "@/components/mobile-shell";
export default async function ActivitiesPage() { const role = await requireRole((item) => item.id === "operario" || item.id === "admin"); return <MobileShell role={role}><main className="space-y-5 px-3 py-4 md:px-6 md:py-6 lg:px-7"><header><p className="text-[0.6875rem] font-bold uppercase tracking-[0.16em] text-blue">Operación audiovisual</p><h1 className="mt-1 text-2xl font-bold tracking-tight">{role.id === "admin" ? "Todas las actividades" : "Mis actividades"}</h1><p className="mt-1 text-sm text-ink-muted">Programadas, en proceso y entregadas.</p></header><ActivityDashboard role={role} /></main></MobileShell>; }
