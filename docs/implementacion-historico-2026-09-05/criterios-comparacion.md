Comparación limpia, modo execute, pipeline core, perfil o/max. Sin herramientas ni acceso al proyecto. Decide entre ambas derivaciones independientes adjuntas, usando solo evidencia suministrada. Marco YA autorizó implementar lo aprobado; no exigir nueva aprobación por decisiones técnicas compatibles. Aunor está expresamente aplazado. No modificar protocolo ni goal.

Convergencia: columna por jornada; lugar general como fallback; días sin horas; dos filas con mismas fechas y distinto lugar, contador por ID único; mantener autoridad/RLS/versiones; frontend v2 fail-closed contra backend antiguo; borradores v5 legibles. Tests en DB temporal separada: PROHIBIDO ejecutar verify-local-supabase.mjs sobre proyecto existente porque resetea datos en inicio y cleanup.

Resolver diferencias concretas: envoltorios v2 SECURITY INVOKER con grants authenticated explícitos que normalicen p_spans y llamen v1 autorizadas, frente a duplicar cuatro cuerpos grandes; normalizar vacíos de forma que omisión y place vacío equivalgan en v2 SIN modificar hashes/idempotencias v1 antiguas. Proponer contrato exacto que mantenga replays legados.

Hallazgo adicional de Codex tras congelar derivación: replace_activity_spans elimina y recrea todas las filas. Si un cliente antiguo llama replan_activity_v1 omitiendo place después de que existan lugares específicos, el coalesce simple borraría esos lugares silenciosamente. Necesitamos fail-closed ante pérdida accidental de metadatos en replan legado o preservación segura, distinguir campo omitido por cliente antiguo de vacío explícito por cliente nuevo. Pregunta: puede el writer actualizado rechazar payload con claves omitidas cuando ya hay span.place no vacío, mientras v2 siempre envía place explícito normalizado? ¿Qué límites/concurrencia requiere? No relajar roles ni hacer fallback a v1 en frontend nuevo.

Salida compacta: decisión técnica aplicable, tradeoffs aceptados, hallazgos realmente bloqueantes y pruebas obligatorias, no reauditoría ni nuevo diseño. Distinguir decisiones UX aprobadas de dudas innecesarias: mostrar lugares junto a jornadas ya cabe en coincidencias aprobadas; campo general opcional hoy y permanece opcional; no inventar requisito global obligatorio. El entorno no permite nuevas preguntas que no cambien alcance.
